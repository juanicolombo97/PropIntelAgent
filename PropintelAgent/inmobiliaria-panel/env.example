# Variables de entorno para el panel inmobiliario
# Copia este archivo a .env.local y configura tus valores

# Secreto para JWT (genera uno único y seguro)
JWT_SECRET=tu-secreto-jwt-super-seguro-cambialo-en-produccion

# === CONFIGURACIÓN DE AUTENTICACIÓN ===

# Credenciales del administrador principal
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123
ADMIN_EMAIL=admin@propintel.com

# === USUARIOS ADICIONALES (Opcional) ===
# Formato: USER_<username>_PASSWORD, USER_<username>_EMAIL, USER_<username>_ROLE
# Ejemplo para usuario "juan":
# USER_JUAN_PASSWORD=password123
# USER_JUAN_EMAIL=juan@propintel.com
# USER_JUAN_ROLE=user

# === CONFIGURACIÓN DE API ===

# URL del API del bot de WhatsApp/Lambda
WHATSAPP_API_URL=http://localhost:8000
# Para AWS Lambda, usar la URL de tu función Lambda:
# WHATSAPP_API_URL=https://tu-lambda-url.execute-api.region.amazonaws.com/prod

# API Key para conectar con el backend (debe coincidir con ADMIN_API_KEY del bot/lambda)
ADMIN_API_KEY=tu-api-key-super-secreta

# === CONFIGURACIÓN PARA PRODUCCIÓN ===

# URL base pública (solo para Next.js público)
# NEXT_PUBLIC_API_BASE=https://tu-lambda-url.execute-api.region.amazonaws.com/prod

# Modo de producción (opcional)
# NODE_ENV=production